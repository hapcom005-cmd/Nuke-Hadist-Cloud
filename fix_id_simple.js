const fs = require('fs'); let code = fs.readFileSync('build_ui_premium_v3.js', 'utf8'); let lines = code.split('\n'); for (let i = 0; i < lines.length; i++) { if (lines[i].includes('class="notranslate"') && lines[i].includes('style="font-family:var(--font-arabic') && lines[i].includes('${a.teksArab}')) { lines[i] = lines[i].replace('<div class="notranslate"', '<div id="text-arab-\\{a.nomorAyat}" class="notranslate"'); } } fs.writeFileSync('build_ui_premium_v3.js', lines.join('\n')); console.log('Fixed!');
